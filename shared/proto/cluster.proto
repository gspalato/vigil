syntax = "proto3";

package vigil;

import "location.proto";

message Cluster {
    string            cluster_id = 1; // UUID
    repeated Location points     = 2;
    Location          centroid   = 3;
    float             area       = 4; // In square kilometers
    float             density    = 5;
    bool              is_noise   = 6;
}