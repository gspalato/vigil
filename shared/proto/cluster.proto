syntax = "proto3";

package vigil;

import "location.proto";

message Cluster {
    string            cluster_id = 1; // UUID
    repeated Location points     = 2;
    Location          centroid   = 3;
    float             radius     = 4; // In meters
    float             density    = 5; // In meters
    bool              is_noise   = 6;
}